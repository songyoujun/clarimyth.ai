# clarimyth.ai

Landing page for **ClariMyth — See What AI Hides.**

Static site served by GitHub Pages from the `main` branch root. Custom domain is set in `CNAME`.
`clarimyth.com` is a separate repo that redirects here.
